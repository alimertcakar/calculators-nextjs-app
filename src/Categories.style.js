import { makeStyles } from "@material-ui/styles"
const style = makeStyles(theme => ({
    categorieItem: { padding: 15 },
    icon: {
        color: theme.palette.primary.main
    },
    catMenuItem: {
        marginTop: 5,
    }
}));
export default style;